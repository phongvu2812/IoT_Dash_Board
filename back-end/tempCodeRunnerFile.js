axios
//    .get(apiUrl)
//    .then((response) => {
//       const dataArray = response.data;

//       // Get an array of attribute names for each object
//       const allAttributeNames = dataArray.map((dataObject) => Object.keys(dataObject));

//       // Flatten the array of arrays into a single array
//       const flattenedAttributeNames = allAttributeNames.flat();

//       // Print the flattened attribute names
//       console.log("Flattened Attribute Names:", flattenedAttributeNames);
//    })
//    .catch((error) => {
//       console.error("Error fetching data:", error.message);
//    });
